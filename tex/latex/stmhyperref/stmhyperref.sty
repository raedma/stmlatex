%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Header                           %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% 
% This file includes hyperref setup
% Based upon the hyperref package:
%   https://ctan.org/pkg/hyperref
%
% Usage
%  - Preamble:
%    - \usepackage{stmhyperref}
% 
% Revisions: 2019-10-27 Martin Raedel <martin.raedel@dlr.de>
%                       Initial draft
%               
% Contact:   Martin Raedel,  martin.raedel@dlr.de
%            DLR Lightweight Systems
%          
%                                 __/|__
%                                /_/_/_/  
%            www.dlr.de/sy/en      |/ DLR
%
% Copyright (C) 2019-... DLR Lightweight Systems
% 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Content                          %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Declare that this style file requires at least LaTeX version 2e.
\NeedsTeXFormat{LaTeX2e}

% Provide the name of your page, the date it was last updated, and a comment about what it's used for
\ProvidesPackage{stmhyperref}[2023/02/12 STMs hyperref setup definitions]

% ---------------------------
% Options
% ---------------------------

% For options
\@ifpackageloaded{kvoptions}{}{\RequirePackage{kvoptions}}%

% Option group
\SetupKeyvalOptions{%
  family=stmhyperref,%
  prefix=stmhyperref@,%
  setkeys=\kvsetkeys,%
}

% Load styles
\DeclareBoolOption[true]{styles}

% Apply style name
\DeclareStringOption[stmdefault]{style}

% Load styles
\DeclareBoolOption[true]{pdfopts}

% Apply style name
\DeclareStringOption[stmdefault]{pdfopt}

% Process options
\ProcessKeyvalOptions{stmhyperref}

% ---------------------------
% Modules
% ---------------------------

% Load the base package
\@ifpackageloaded{hyperref}{}{%
  \RequirePackage{stmhyperrefbase}
}%

% Load the styles
\ifstmhyperref@styles
  \@ifpackageloaded{stmhyperrefstyles}{}{%
    \RequirePackage[
      style=\stmhyperref@style
    ]{stmhyperrefstyles}%
  }%
\fi

% Load the styles
\ifstmhyperref@pdfopts
  \@ifpackageloaded{stmhyperrefpdfopts}{}{%
    \RequirePackage[
      style=\stmhyperref@pdfopt
    ]{stmhyperrefpdfopts}%
  }%
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% That's it                        %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Finally, we'll use \endinput to indicate that LaTeX can stop reading this file. LaTeX will ignore anything after this line.
\endinput